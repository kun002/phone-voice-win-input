# 手机语音输入到 Windows：阶段路线

## 总目标

把当前工具从“手机网页提交文本，Windows 粘贴”升级成一个更顺手的局域网输入助手：

- 手机端尽量像轻量 App：扫码连接、可安装到主屏幕、断线自动重连。
- 电脑端尽量像小工具：显示二维码、连接状态、保护剪贴板、后续支持托盘/GUI。
- 输入链路尽量稳：先保证 iOS/Android 都可用，再逐步增强 WebSocket 实时预览、自动同步写入和目标锁定。

## 关键原则

- 不破坏 Windows 剪贴板：手机输入时会临时使用剪贴板完成粘贴，但粘贴后应恢复原剪贴板。
- 不覆盖用户新修改：如果用户在手机粘贴期间或粘贴后，在 Windows 上复制/粘贴了另一段文本，程序不能用旧内容覆盖用户的新剪贴板。
- 先稳后快：默认链路只用手机系统输入法麦克风，手机页面作为隐藏采集承载区，不显示正文、不负责发送；网页语音和第三方语音以后单独评估。
- 局域网优先：先不依赖公网账号、云服务或手机 App。

## 剪贴板保护策略

这是必须优先设计好的底层能力。

流程建议：

1. 电脑端在执行手机文本粘贴前，读取并保存当前 Windows 剪贴板内容。
2. 电脑端把手机文本写入剪贴板。
3. 电脑端向当前焦点窗口发送 `Ctrl+V`。
4. 粘贴完成后，电脑端检查当前剪贴板是否仍然等于刚才写入的手机文本。
5. 如果仍然相同，说明用户没有在这段时间改剪贴板，恢复步骤 1 保存的原内容。
6. 如果不同，说明用户已经在 Windows 上复制/粘贴了新的内容，程序放弃恢复，保留用户的新剪贴板。

这个策略可以保证：

- 手机语音输入不会长期污染 Windows 剪贴板。
- 用户在 Windows 上复制的新内容不会被程序“恢复旧剪贴板”误覆盖。
- 多次手机输入之间，Windows 用户自己的剪贴板变化应被尊重。

## 阶段 1：二维码 + 连接状态

目标：让手机打开页面更省事，并让电脑端知道手机是否连上。

电脑端：

- 启动后生成访问 URL，并显示二维码。
- URL 包含电脑局域网 IP、端口、token。
- 保留纯文本 URL，方便复制。
- 显示当前服务状态：监听地址、端口、token 状态。
- 显示最近连接状态：未连接、已连接、最后心跳时间、手机浏览器信息。

手机端：

- 扫码打开页面。
- 页面加载后向电脑端注册一次连接。
- 页面显示连接状态：连接中、已连接、断线、重连中。

验收标准：

- 用户启动电脑端后，不需要手打 IP，手机扫码即可打开。
- 手机打开页面后，电脑端能看到“已连接”。
- 手机关闭页面或断网后，电脑端能看到连接超时。

## 阶段 2：剪贴板保护

目标：手机输入不破坏 Windows 原本剪贴板，也不覆盖用户后续在 Windows 上复制的新内容。

电脑端：

- 实现“粘贴前保存，粘贴后条件恢复”的剪贴板保护策略。
- 增加开关：保护剪贴板，默认开启。
- 增加失败提示：读取剪贴板失败、写入失败、恢复跳过、恢复成功。

手机端：

- 发送文本时无需额外感知剪贴板保护。
- 手机端无需显示粘贴结果；发送、清空、错误和剪贴板保护结果集中显示在电脑端状态页 / GUI。

验收标准：

- 手机发送文本后，Windows 剪贴板恢复为发送前内容。
- 如果手机发送后用户马上在 Windows 复制了另一段文本，程序不能把它恢复成旧内容。
- 连续多次手机输入时，每次都只临时占用剪贴板。

## 阶段 3：WebSocket 实时通道

目标：从一次性 HTTP 提交升级到双向实时连接，为连接状态、目标锁定、电脑端实时预览和清空通知铺路。

电脑端：

- 提供 WebSocket endpoint。
- 维护设备会话：device id、连接时间、最后心跳、浏览器信息。
- 接收手机端消息：文本更新、目标锁定、目标释放、心跳。
- 向手机端回传状态：已收到、错误、目标锁状态、清空序号。
- 保留 HTTP 提交作为 fallback。

手机端：

- 页面打开后建立 WebSocket。
- 断线自动重连。
- 隐藏输入承载区内容变化时，通过 WebSocket 发送更新。
- 不在手机端提供发送按钮，最终发送由电脑端状态页 / GUI 完成。
- 显示电脑端回传状态。

消息建议：

```json
{"type":"hello","deviceName":"iPhone Safari","deviceId":"..."}
{"type":"text.update","text":"当前完整文本","clearSequence":1}
{"type":"heartbeat","time":1234567890}
{"type":"status","ok":true,"clearSequence":2}
```

验收标准：

- 手机页面不刷新也能实时显示连接状态。
- 电脑端能看到手机端正在输入的内容预览。
- WebSocket 断开后可以自动恢复。
- WebSocket 不可用时，仍可用 HTTP 预览更新 fallback；清空通知优先依赖 WebSocket 心跳。

## 阶段 4：PWA 外壳

目标：手机端像 App 一样打开，兼容 iOS 和 Android。

电脑端：

- 提供 `manifest.webmanifest`。
- 提供基础图标。
- 提供 Service Worker 缓存页面壳。
- 注意局域网 HTTP 下的 PWA 能力限制，必要时提供降级说明。

手机端：

- 页面适配小屏幕和安全区域。
- 提供“添加到主屏幕”的提示。
- 保存最近一次成功连接的 URL、token、device id。
- 打开 PWA 后自动尝试重连最近的电脑。

验收标准：

- Android Chrome 可以添加到主屏幕。
- iOS Safari 可以添加到主屏幕并以独立窗口打开。
- 手机再次打开时，会自动连接最近一次电脑地址。
- 如果电脑 IP/端口变化，页面显示重新扫码提示。

## 阶段 5：轻 GUI / 托盘

目标：电脑端不再依赖一直盯着 PowerShell。

电脑端：

- 提供轻量 GUI 窗口。
- 显示二维码、URL、连接设备、预览片段和目标锁定状态。
- 提供按钮：复制链接、重生成 token、暂停接收、发送预览、清空预览、测试粘贴、退出。
- 提供设置：保护剪贴板、原生控件后台写入、自动切回目标窗口、目标位置回点、粘贴后回到原窗口、目标锁超时。
- 提供托盘图标：显示/隐藏窗口、复制链接、暂停/恢复、退出。
- 后续可做开机自启，但默认不强制。

手机端：

- 保持纯采集，不新增手机端设置；只显示连接、目标状态和传输字数。

技术候选：

- 最轻方案：Python `tkinter` 做窗口，Windows 下用 `Shell_NotifyIcon` 做基础托盘。
- 界面更舒服：Python `pywebview`，电脑端 GUI 也用网页渲染。
- 产品化方案：Tauri 或 Electron，但打包复杂度更高。

验收标准：

- 用户双击启动后看到可视化窗口和二维码。
- 关闭窗口时可以选择最小化到托盘。
- 不打开 PowerShell 也能知道手机是否连接。

## 阶段 6：采集模式与目标锁定增强

目标：让长文本输入更流畅，同时避免多 Agent / 多窗口切换时输错目标。

基础输入链路：

- 手机端作为纯采集器：隐藏输入承载区接收系统输入法麦克风文字，不显示正文。
- 手机端把识别文字作为传输流上传，电脑端默认自动镜像到锁定的 Windows 输入框。
- 自动同步默认只发送新增后缀，不再用手机全文反复替换 Windows 输入框；如果手机输入法修正了当前活动段最近一小段文字，自动链路会做尾部修正。输入暂停一小会儿后，前面内容进入保护区；如果手机输入法再回头动到保护区，工具把当前位置当成新段起点，后续语音从新段继续同步，避免大面积覆盖用户在电脑输入框里的手工修改。
- 电脑端状态页 / GUI 保留“发送预览”和“清空预览”作为兜底：前者重新写入目标输入框，后者丢弃当前隐藏缓存。
- 电脑端清空预览、释放目标，或手机端一段输入空闲收尾后，通知手机端清空隐藏输入缓存，避免下一段带上上一段。

目标锁定：

- 在一次手机输入开始时，电脑端记录当时的 Windows 焦点窗口/控件作为锁定目标。
- 输入未完成前，即使用户临时切换去看别的窗口，发送时仍尽量回到锁定目标输入。
- 如果 Windows 能提供输入光标位置，锁定时记录目标相对位置，粘贴/撤销前短暂回点原位置，减少同一窗口内误发到其他输入框。
- 电脑端清空预览、手动释放目标、手机页面离开、手机端空闲收尾或超时后释放锁定目标，下一段输入再重新锁定；基础版会在手机页面刷新/关闭时尽量主动释放，手机端默认 5 秒无新识别文本后自动收尾，服务端默认 5 分钟无手机文本活动兜底释放，可在状态页/GUI 调整或关闭服务端兜底。
- 基础版通过 Win32 前台窗口/焦点控件快照恢复目标；如果目标程序是管理员权限，工具也可能需要管理员权限。
- 平滑写入基础版会优先尝试 Windows 原生 `Edit` / `RichEdit` / WinForms Edit 控件的后台写入；不支持的目标默认自动回退到恢复目标 + 目标位置回点 + 剪贴板粘贴。用户也可以关闭“自动切回目标窗口”，此时普通粘贴会尝试后台粘贴消息，不能安全后台替换全文时提示失败，避免抢前台和误输入。

安全交互：

- 锁定目标必须有明显状态提示，并在电脑端提供释放入口。
- 手机端保持纯采集面板，不提供正文显示、设置、历史、快捷标点、发送上一句、撤销或暂停按钮；这些控制集中到电脑端状态页 / GUI。
- 自动同步或电脑端发送预览前必须有锁定目标；没有目标时拒绝写入。

后续语音扩展：

- 当前手机网页不调用 Web Speech API，避免 iOS/Android 行为不一致和 `not-allowed`。
- 如需第三方语音识别，后续做成单独可选通道，不影响系统输入法麦克风主链路。

验收标准：

- 手机端识别文字不在手机端显示，会自动同步到锁定的 Windows 输入框，并同时进入电脑端预览。
- 用户在一次输入过程中临时切换窗口时，不会把内容误发到新焦点输入框；恢复失败时必须拒绝粘贴。
- 用户可以在电脑端释放目标锁定，或在电脑端清空预览后自动释放目标，下一次输入重新选择目标。
- 手机页面刷新、关闭、断开或长时间无文本活动后，旧目标锁会释放，不会长期占用激活输入设备。
- iOS/Android 都继续使用系统键盘语音输入。

## 后续体验清单

- 电脑端集中设置：暂停/恢复、释放目标、测试粘贴、发送预览、清空预览、重生成 token。
- 手机端纯采集：触控采集区、连接/目标状态、传输字数；不显示正文。
- 目标锁定：开始输入时锁定目标，完成后释放；基础版已支持窗口/焦点恢复、原生控件后台写入、可选自动切回目标窗口、可选目标位置回点、实验性粘贴后回到原窗口、页面离开主动释放和目标锁超时释放，降低多 Agent 和同窗口多输入框误输入概率。
- 暂停/恢复接收：电脑端可快速暂停，暂停期间服务端拒绝写入 Windows。
- 最近错误摘要：保留在电脑端状态页 / GUI，不在手机端保存最近发送历史。
- 更平滑目标写入：基础版已支持部分原生文本控件后台写入；后续可继续探索 UI Automation，扩大对浏览器/Electron 等目标的覆盖。
- 多设备：基础版已允许多个手机连接和预览，但同一时间只有一个激活输入设备能写入 Windows。
- 设备识别：手机端自动上报 iPhone / iPad / Android / 手机浏览器等基础名称，并在状态页和 GUI 显示。
- 防火墙提示：基础版已在启动终端、电脑状态页和 GUI 显示连接诊断，提示检查同网段、绑定地址和 Windows 防火墙。
- 自动发现：后续可考虑 mDNS/局域网广播，减少扫码依赖。
- 日志：基础版已在状态页和 GUI 显示最近错误摘要，只记录设备、动作、字符数和原因，不记录完整敏感文本。
- 热键：基础版已支持可选 Windows 全局快捷键暂停/恢复接收，例如 `ctrl+alt+p`。

## 推荐实施顺序

1. 阶段 1：二维码 + 连接状态。
2. 阶段 2：剪贴板保护。
3. 阶段 3：WebSocket 实时通道。
4. 阶段 4：PWA 外壳。
5. 阶段 5：轻 GUI / 托盘。
6. 阶段 6：采集模式与目标锁定增强。

这个顺序的好处是：先解决每天都会遇到的连接和剪贴板问题，再升级实时体验，最后做更完整的 App 化体验。

## 当前实施状态

- 阶段 1 已完成：启动终端二维码、手机连接状态、电脑状态页已实现。
- 阶段 2 已完成基础版：默认保护文本剪贴板；如果 Windows 用户已经复制了新文本，程序不会恢复旧内容覆盖它。
- 阶段 3 已完成基础版：手机端优先使用 WebSocket，断线自动重连；HTTP 提交仍作为 fallback；电脑状态页和 GUI 可显示截断预览片段。
- 阶段 4 已完成基础版：已提供 manifest、图标、Service Worker 注册和手机端 PWA 元信息；manifest 启动地址会保留已配对 token，方便从主屏幕再次打开。A 方案下手机端不保存历史或可见正文；局域网 HTTP 下不同浏览器的 PWA 能力会有差异；如果电脑 IP、端口或 token 变化，仍需重新扫码。
- 阶段 5 基础版完成：已提供 `--gui` 轻量窗口，显示二维码、URL、设备状态、目标锁定状态和运行设置；GUI/状态页可暂停接收、释放全部目标、发送预览、清空预览、重生成 token，并对已锁定目标执行测试粘贴；状态页还支持按单台设备释放目标。GUI/状态页可切换剪贴板保护、原生控件后台写入、自动切回目标窗口、目标位置回点、粘贴后回到原窗口和目标锁超时。Windows 下已提供基础系统托盘：显示/隐藏窗口、复制链接、打开状态页、暂停/恢复、退出；托盘不可用时降级为任务栏最小化。启动终端、GUI 和状态页已提供基础网络诊断，手机连不上时提示检查同网段、绑定地址和防火墙。启动时可选 `--hotkey` / `-Hotkey` 启用全局热键暂停/恢复接收。
- 阶段 6 基础版完成：已移除网页语音/连续网页语音，当前只走系统输入法麦克风；手机端已收敛为纯采集面板，没有可见正文输入框和发送按钮，只显示采集入口、连接/目标状态和传输字数。手机端识别结果会自动同步到锁定的 Windows 输入框，同时上传到电脑端预览；发送预览、清空预览、暂停/恢复、释放目标、测试粘贴、重生成 token 等操作集中在电脑状态页 / GUI。目标锁定会优先尝试对原生文本控件后台写入，不能后台写入时默认通过 Win32 恢复前台目标，并在可用时回点锁定时记录的输入位置后粘贴；粘贴后回到原窗口保留为实验项，默认关闭。如果关闭“自动切回目标窗口”，普通粘贴会尝试后台粘贴消息。刷新/重连会恢复目标锁状态，页面离开会尽量主动释放目标锁，长时间无手机文本活动会自动释放旧锁。多设备基础版已完成：多台手机可连接和预览，但只有一个激活输入设备能写入。更高级的 UI Automation 覆盖和跨设备段落队列管理仍保留为下一步。
